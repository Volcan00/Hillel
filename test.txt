CREATE TABLE "currency" ("bank"	TEXT NOT NULL, "currency"	TEXT NOT NULL, "date_exchange"	TEXT NOT NULL, "buy_rate"	REAL NOT NULL, "sale_rate"	REAL NOT NULL, "id"	INTEGER NOT NULL UNIQUE, PRIMARY KEY("id" AUTOINCREMENT))